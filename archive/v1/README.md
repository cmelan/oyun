# Archived v1

This directory contains the frozen legacy single-file edition of the game.
It is retained only as historical reference and must not be used by Render.

To run its original regression suite from this directory:

```bash
npm test
```

The active Vite application now lives at the repository root.
